# Datepicker

This is a component which is normally used on the client side. But it should be renderable from server-side too.

## Mini Calendar

There should be a seperate mini-calendar component, which can be wrapped by a datepicker component. This also allows for ranges, etc.
